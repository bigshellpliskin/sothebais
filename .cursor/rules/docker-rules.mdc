---
description: Rules on how to use docker, docker compose and assoc. files
globs: 
---

# Rules on how to use docker for this project
- Use `docker compose` instead of `docker-compose`.
- Main docker compose file is [compose.yaml](mdc:compose.yaml).
- Individual dockerfiles will be in `apps/[service_name]/Dockerfile`.

- You can @ files here
- You can use markdown but dont have to
